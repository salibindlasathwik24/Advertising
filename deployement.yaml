apiVersion: apps/v1
kind: Deployment
metadata:
  name: adversting-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: advertising
  template:
    metadata:
      labels:
        app: advertising
    spec:
      containers:
        - name: car-emission
          image: sathwik10/advertising
          ports:
            - containerPort: 5000
